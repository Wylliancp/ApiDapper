CREATE PROCEDURE spGetCustomerOrdersCount
AS
	SELECT
		C.[ID],
		CONCAT(C.[FIRSTNAME],'',C.[LASTNAME]) AS NAME,
		C.[DOCUMENT],
		COUNT(O.[ID]) AS QUANTIDADEPEDIDO
	FROM
		[CUSTOMER] C
	INNER JOIN [ORDER] O ON C.ID = O.CUSTOMERID
	GROUP BY C.ID, NAME, C.DOCUMENT



	